fn foo(u8, u16 a) {

}

struct a {
	char a = 1;
};

class Myclass : pub a, priv b {
pub:
	priv Myclass() {
		
	}

	fn public_function() {

	}

	u8 pubdata;

priv:
	fn private_function() {

	}

	u8 privdata;
};

pub:
u64 myval = 20+10;
priv:
char priv_val = 2;

pub enum Enumlist {
	ENUM1 = 1,
	ENUM2,
	ENUM3,
	u8 MyStruct() {

	},

	u32 foo(),

	Tuple(u8)
};

b(1, (a+1+a+b) * (2/10));

test: a;

myblock {
 


	yes {
		a+1
	}
}

if(a) {
	pub:

	1+2
}

u8 a,b,c = (char)(b+1);

wholeif:

if(b) truebranch {
	if(2) anothertruebranch {

	} else elsebranch {

	}
} else {

	if(2) anothertruebranch {

	} else elsebranch {

	}
}

switch(u8) {
	@1+2,3: { do(1); do2(2);
	char c;
		if(1) {

		}

	}

	@2: again(3);
	@u8: a();
}

switch(u8) {
	@1:
		char i;
		test();
		1

	@2+1,23: {
		u32 b;

	},
	@foo(1,2), bar(3,4): {

	}
}

char a = if(1) {};

char c = switch(a) { @1: a * n, @2: a*n, (n,n2)@3,2: a*n*n2  };

switch(var) {
	@1: one();
	@2: two();
	@3: 
		three();
		another();

	@4: {
		four();
		another();
	}
	@5,6,7:
		five();
		six();
		seven();

	@u8: isU8();

	@equalto(): lambdaMatch();

	@else: {
		none();
	}
	@:
		none();
}